# Tilt runtime dependencies.
# pandas-ta 0.4.x is the Python 3.12-compatible line; it requires NumPy >= 2.2.
# pandas >= 2.2 is the first pandas line with full NumPy 2 support.

# --- Indicators (step 3) ---
numpy>=2.2,<3.0
pandas>=2.2,<3.0
pandas-ta==0.4.71b0
setuptools>=68.0

# --- Market data layer (step 4) ---
yfinance>=0.2.40
pyarrow>=15.0   # parquet engine for pandas

# --- FastAPI (step 8) ---
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
httpx>=0.27  # used by fastapi.testclient

# --- LLM narrative layer (v3) ---
# Optional at runtime — if missing or no GEMINI_API_KEY, the deterministic
# fallback in tilt.llm.provider takes over. Pinning a floor only.
google-genai>=1.0

# --- Test (step 3) ---
pytest>=8.0
