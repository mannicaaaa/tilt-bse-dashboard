# Tilt — environment template. Copy to `.env` (never commit `.env`).

# --- Groww API (deferred until deploy-time; mock provider used until then) ---
GROWW_API_KEY=
GROWW_API_SECRET=

# --- Provider selection (defaults shown) ---
# MARKET_DATA_PROVIDER=snapshot      # one of: snapshot, yfinance
# PORTFOLIO_PROVIDER=mock            # one of: mock, empty, groww

# --- FastAPI ---
# HOST=0.0.0.0
# PORT=8000
